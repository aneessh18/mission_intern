#segment tree is for getting the range sums and also to do the updates.
#for normal range sums segment trees are sufficient
#but for updates and queries lazy propogation will be an efficient algorithm
class SegmentTree:
    def buildtree(a, lo, hi):
        if lo == hi:
            
    def __init__(self, A):
        self.A = A
